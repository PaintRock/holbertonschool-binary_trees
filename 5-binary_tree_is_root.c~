#include "binary_trees.h"
/**
* binary_tree_is_root -- fx that checks to see if it is a root
*@node: the one to check
* Return: 1 if it is a root, 0 if it is not
* 
*/
int binary_tree_is_root(const binary_tree_t *node)
{
        if (node == NULL)
        return (0);

        if (node->parent == NULL)
        return (1);

        else
}